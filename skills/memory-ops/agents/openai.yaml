interface:
  display_name: "Memory Ops"
  short_description: "Stages and promotes durable memory"
  default_prompt: "Use $memory-ops to decide whether new information should stay task-local, be staged as a candidate, or be promoted into long-term memory, and identify the correct memory bucket."
