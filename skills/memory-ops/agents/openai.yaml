interface:
  display_name: "Memory Ops"
  short_description: "Stages and promotes durable memory"
  default_prompt: "Use $memory-ops after implementation and verification to summarize meaningful task signals, compare them against current memory, classify each as temporary, candidate, durable, stale, duplicate, or noise, and return an explicit memory-updated or no-memory-change-needed decision."
